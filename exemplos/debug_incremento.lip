vazio testeIncremento() {
    inteiro contador = 0;
    imprima "Contador inicial: " + paraTexto(contador);
    contador++;
    imprima "Contador após ++: " + paraTexto(contador);
}
testeIncremento();
