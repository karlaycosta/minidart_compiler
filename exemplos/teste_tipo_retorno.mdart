// Teste para validação de tipo de retorno de função
// A função declara retornar inteiro mas retorna real - deve gerar erro

inteiro teste(inteiro a) {
    retorne a + 2.5; // Erro: retornando real quando deveria ser inteiro
}

imprima "Testando validação de tipo...";
var resultado = teste(10);
imprima "Resultado: " + resultado;
