name: minidart_compiler
description: Um compilador completo para a linguagem MiniDart com sintaxe em português.
version: 1.12.7
repository: https://github.com/karlaycosta/minidart_compiler

environment:
  sdk: ^3.8.1

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3

# Informações para extensão VS Code
# Para criar a extensão, execute:
# npm install -g yo generator-code
# yo code (selecionar "New Language Support")
# Integrar com este compilador usando os comandos do bin/compile.dart
