// Teste de recursividade - Fatorial
inteiro fatorial(inteiro n) {
    se (n <= 1) {
        retorne 1;
    } senao {
        retorne n * fatorial(n - 1);
    }
}

var resultado = fatorial(5);
imprima resultado;  // Deve imprimir 120
